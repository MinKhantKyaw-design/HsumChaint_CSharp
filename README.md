# HsumChaint_CSharp
# Scaffold_Script
- dotnet ef dbcontext scaffold "Server=.;Database={Your DB Name};User Id={your user ID};Password={your password};TrustServerCertificate=True;" Microsoft.EntityFrameworkCore.SqlServer -o Models -c AppDbContext -f

# Docker Command
- docker compose up --build 

